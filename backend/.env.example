# =================================================================
# MindBridge AI - Environment Configuration
# =================================================================
# Copy this file to .env and fill in your actual API keys
#
# WHY DO WE NEED EACH OF THESE?
# =================================================================

# -----------------------------------------------------------------
# 1. OPENROUTER_API_KEY (REQUIRED - Core AI Models)
# -----------------------------------------------------------------
# This is how we access NVIDIA Nemotron models via OpenRouter
#
# What is OpenRouter?
# - A unified gateway to access multiple LLM providers with ONE API key
# - Instead of signing up for NVIDIA, OpenAI, Anthropic separately,
#   OpenRouter gives us access to ALL of them
#
# Why not use NVIDIA directly?
# - OpenRouter provides easier access during development
# - Same Nemotron models, but simpler authentication
# - Free tier available for testing
#
# Get your key: https://openrouter.ai/keys
# Free credits: You get $1 free to start
OPENROUTER_API_KEY=your_openrouter_key_here

# -----------------------------------------------------------------
# 2. TAVILY_API_KEY (REQUIRED - Web Search for Resources)
# -----------------------------------------------------------------
# Our Resource Matching Agent needs to find therapists, support groups,
# crisis hotlines, and mental health resources
#
# What is Tavily?
# - A search API specifically designed for AI agents
# - Returns structured data that LLMs can easily understand
# - Better than Google Search API for our use case
#
# Why not just use Google?
# - Tavily formats results for LLM consumption
# - Filters out ads and irrelevant content
# - Designed for RAG (Retrieval Augmented Generation)
#
# Get your key: https://app.tavily.com/sign-in
# Free tier: 1,000 searches/month
TAVILY_API_KEY=your_tavily_key_here

# -----------------------------------------------------------------
# 3. SUPABASE (OPTIONAL - For Persistence)
# -----------------------------------------------------------------
# Stores user data, habits, therapist profiles based on privacy tier
#
# What is Supabase?
# - Open-source Firebase alternative
# - Provides PostgreSQL database + real-time subscriptions + auth
#
# Why Supabase?
# - Free tier is generous (500MB database)
# - Built-in user authentication
# - Real-time updates (perfect for live habit tracking)
# - Easy to set up privacy tiers
#
# For hackathon: We can start WITHOUT this and use in-memory storage
# For production: Absolutely needed
#
# Get started: https://supabase.com/dashboard
SUPABASE_URL=your_supabase_project_url
SUPABASE_KEY=your_supabase_anon_key

# -----------------------------------------------------------------
# 4. REDIS (OPTIONAL - For Agent State Management)
# -----------------------------------------------------------------
# Manages conversation state and agent coordination
#
# What is Redis?
# - In-memory data store (super fast)
# - Perfect for storing "current state" of agents
#
# Why Redis?
# - LangGraph agents need to track their state between steps
# - Example: Crisis Agent needs to remember "user mentioned suicide"
#   across multiple conversation turns
#
# For hackathon: We can use LangGraph's in-memory state
# For production: Redis ensures agents don't lose context
#
# Get started: https://redis.io/try-free/ (free 30MB)
REDIS_URL=redis://localhost:6379

# -----------------------------------------------------------------
# 5. Development Settings
# -----------------------------------------------------------------
# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Environment: development, staging, production
ENVIRONMENT=development

# Model choices (can be changed based on task complexity)
COORDINATOR_MODEL=nvidia/nemotron-super-49b-v1_5:free
AGENT_MODEL=nvidia/nemotron-nano-9b-v2:free
